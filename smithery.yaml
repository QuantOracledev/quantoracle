startCommand:
  type: http
  configSchema:
    type: object
    properties: {}
    description: "No configuration required. 1,000 free calls/day with no API key."
  url: "https://mcp.quantoracle.dev/mcp"
