name = "quantoracle"
main = "src/index.ts"
compatibility_date = "2024-12-01"

[vars]
BACKEND_URL = "https://your-droplet-ip-or-internal-domain:8000"
# Replace with your actual droplet IP after deploy

# Custom domain routing
# After deploy, add custom domain in Cloudflare dashboard:
# Workers & Pages > quantoracle > Settings > Domains & Routes
# Add: api.quantoracle.dev
